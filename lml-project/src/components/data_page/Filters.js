// Filters.js renders a multitude of preferences for filtering 
// through the visual debris data. 
// Selected filters will be passed upwards to DataVis.js for use in Display.js

// Filters.js is rendered by DataVis.js, and currently renders no children.

import React from 'react';

class Filters extends React.Component {
    
    render() {     
      return (
        // html goes here
        <div>

        </div>
      );
    }
}

export default Filters